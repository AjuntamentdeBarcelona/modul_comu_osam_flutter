enum VersionControlResponse {
  ACCEPTED,
  DISMISSED,
  CANCELLED,
  ERROR,
  NOT_NEEDED,
}

enum RatingControlResponse {
  ACCEPTED,
  DISMISSED,
  CANCELLED,
  ERROR,
  NOT_NEEDED,
  HANDLED_BY_SYSTEM
}

enum Language { CA, ES, EN }
